{-
    Takes in a number and puts it in a bucket. The buckets are labeled 0, 1... 5. 
        If input is 20 or less it goes in bucket 0, 21...40 -> bucket 1 and so on
        If the input is greater than 100 it goes in bucket 5
    returns the bucket number

    Author: Youssef Amin
    Input: in
    output: out
-}

-- testing both types of comments here 
n := in;

if n > 100 then
    y := 5
else
    if not [n <= 20] then
        if n < 41 then
            y := 1
        else
            if [n > 80 and n <= 100] then
                y := 4
            else
                if [n < 51 or n <= 60] then
                    y := 2
                else
                    y := 3
                fi
            fi
        fi
    else
        y := 0
    fi
fi;

out := y
