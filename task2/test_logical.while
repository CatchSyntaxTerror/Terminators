{-
  This program tests the use of:
  - not, and, or
  - skip
  - comparison operators (=, <, <=, >, >=)

  Author: Zhuo Li
  Input: a single integer x
  output: result
-}

x := input;
y := 10;
z := 0;

-- Test combined boolean expressions
if not [x < y and x >= 0 or x = 0] then
  z := 1;
  skip
else
  z := 2;
fi;

{- Test comparisons and boolean ops-}
if [x <= y and not [y > 20 or z = y]] then
  result := 100
else
  result := z
fi;

output := result
